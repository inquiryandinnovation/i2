/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"images.unsplash.com",
			"i2.today",
			"images.theconversation.com",
			"cdn.britannica.com",
		],
	},
};

export default nextConfig;
